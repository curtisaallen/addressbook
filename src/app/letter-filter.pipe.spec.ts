import { LetterFilterPipe } from './letter-filter.pipe';

describe('LetterFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new LetterFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
